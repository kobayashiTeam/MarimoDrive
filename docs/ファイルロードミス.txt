skyback,skyfront画像を用意して前景、後景として利用する
その際skybackファイルのロードを忘れた。
そのままrace中に描画のために使おうとすると、
空っぽのImageからテクスチャを生成、それを描画しようとする

ここが重要。本来絶対描画されないところに別のspriteが描画された。

説明によると、空っぽのテクスチャを描画しようとすると、未定義テクスチャIDとみなして、
GPUは直前に参照していたテクスチャデータを描画しようとする　らしい
だから場所は関係なく直前フレームで描画していた